// components/todo.js
import Note from './note.js'; // Ensure correct path based on your file structure
import Todo from './todo.js'; // Ensure correct path based on your file structure

class Project {
    constructor(name, notes, todos) {
        this.name = name;
        this.notes = notes;
        this.todos = todos;
    }
    // Other methods or properties specific to Todo class
}

export default Project;